# EC-CUBE 4.3 プラグイン開発ブログ

## 概要

EC-CUBE 4.3 のプラグイン開発に関する技術記事を Zenn に投稿するリポジトリです。

## ワークフロー

1. **テーマ決定**: ユーザーが記事のテーマを指定する
2. **記事作成**: `articles/` ディレクトリに Zenn 形式で記事を作成する
3. **AI臭チェック**: 記事を書き終えたら, 必ず最後に `stop-ai-slop-jp` スキルを実行して推敲する（詳細は「仕上げ（AI臭チェック）」を参照）
4. **プルリクエスト**: 記事完成後, GitHub にプルリクエストを送る
5. **公開**: ユーザーがマージすると Zenn に自動投稿される
6. **ブランチ削除**: マージ後, リモートとローカルのブランチを削除する

## 記事作成ルール

- 記事は必ず `articles/` ディレクトリ内に作成する（このディレクトリ内のファイルのみ Zenn に投稿される）
- ファイル名: 半角英数字とハイフンのみ（12〜50文字）
- フロントマター:
  ```yaml
  ---
  title: "記事タイトル"
  emoji: "適切な絵文字"
  type: "tech"
  topics: ["eccube", "eccube4", "php", "symfony"]
  published: true
  ---
  ```
- **対象バージョンは記事ごとに判断する**（詳細は「対象バージョンの判断基準」を参照）
- コード例は実際に動作するものを記載する
- 記事の冒頭（フロントマターの直後）に以下を必ず追加する（お仕事募集バナー → 注意書きの順）。`{対象バージョン}` は記事ごとに判断して埋める:
  ```markdown
  :::message alert
  ## 🙋‍♂️ EC-CUBE 開発・カスタマイズのお仕事, 募集しています！

  プラグイン開発・バージョンアップ・機能追加など, EC-CUBE に関することならお気軽にご相談ください。

  👉 **[お問い合わせはこちら](https://a-zumi.net/contact/)**
  :::

  :::message
  この記事は EC-CUBE {対象バージョン} 以上を対象としています。
  また, [Claude Code](https://claude.ai/claude-code) を使って書かれています。内容に誤りがある場合はコメントでお知らせください。
  :::
  ```
- 記事の末尾に以下のCTAを必ず追加する:
  ```markdown
  ---

  ## 📩 EC-CUBE開発・カスタマイズのご相談

  以下のような案件, お気軽にご相談ください。

  - プラグイン開発・既存プラグインの改修
  - EC-CUBE 4系へのバージョンアップ対応
  - カスタマイズ・機能追加

  👉 **[お問い合わせはこちら](https://a-zumi.net/contact/)**

  ---
  ```

## 対象バージョンの判断基準

記事を書く前に, 扱う機能・クラス・設定が**どのバージョンから使えるか**を必ず確認し, 注意書きの `{対象バージョン}` を決める。

判断の手順:

1. 記事で扱うクラス・メソッド・設定項目を, EC-CUBE リポジトリの `4.3` ブランチと `4.4` ブランチの両方で確認する
   - 例: `gh api repos/EC-CUBE/ec-cube/contents/<path>?ref=4.3` で 404 なら 4.3 には存在しない
2. 4.3 に存在しない, または 4.3 では挙動が違うなら **4.4**
3. 4.3 でも同じように動くなら **4.3**

| 記事の内容 | 対象バージョン |
| --- | --- |
| 4.4 で追加・変更された機能の解説 | 4.4 |
| 4.4 の破壊的変更・移行手順 | 4.4 |
| 4.3 でも動く実装・カスタマイズ | 4.3 |
| 4.3 向けの実装だが 4.4 の動向にも触れる | 4.3（本文で 4.4 に言及する） |

未リリースバージョンを扱う場合は, 注意書きとは別に本文で「現時点で未リリース」「`4.4` ブランチにマージ済み」と明記する。

ロードマップなど複数バージョンにまたがる記事は, テンプレートに縛られず実態に合った文面を書いてよい。

## 仕上げ（AI臭チェック）

記事の執筆・編集が終わったら, **最後に必ず `stop-ai-slop-jp` スキルを実行する**。

- スキルの場所: `.claude/skills/stop-ai-slop-jp/`
- 実行タイミング: 記事本文が完成した後, プルリクエストを送る前
- 対象: 記事本文（コードブロック, フロントマター, 定型のバナー・CTA は対象外）
- チェック観点: 主体の不在, 命題型H2, 両論併記, 全角ダッシュや中黒並列などの記号アーティファクト, 3項目並列, 偏愛語, リズムの均一さ
- 記事を修正した場合も, 修正後にもう一度実行する

## 参考ドキュメント

以下のドキュメントを必ず参照すること:

- **EC-CUBE 4 開発者向けドキュメント**: https://doc4.ec-cube.net/
  - プラグイン仕様
  - Controller / Entity / Repository カスタマイズ
  - テンプレートカスタマイズ
  - セキュリティガイドライン

## ソースコード確認

記事を作成する際は, 以下のリポジトリのソースコードを確認して正確な情報を記載すること:

- **EC-CUBE 4**: https://github.com/EC-CUBE/ec-cube
- **Symfony**: https://github.com/symfony/symfony

コードの実装詳細やクラス名, メソッド名などは実際のソースコードを参照して記述する。

## アカウント情報

- **Zenn アカウント**: `kurozumi`
- **記事 URL の形式**: `https://zenn.dev/kurozumi/articles/{slug}`
- 記事間のリンクを生成する際は必ず `zenn.dev/kurozumi` を使用すること（`a_zumi` は誤り）

## 注意事項

- 判断に迷ったら「ユーザー価値」で決める
- セキュリティを考慮したコードを書く
- 公式ドキュメントの内容と矛盾しないようにする
- Zennには投稿数の上限（レートリミット）があるため, 1日に複数記事を投稿する場合は注意
- 上限に達した場合は24時間後に再投稿可能
